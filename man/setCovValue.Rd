% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cov.R
\name{setCovValue}
\alias{setCovValue}
\alias{setCovValue.default}
\alias{setCovValue.matrix}
\title{The covariance, method name and options a result installs}
\usage{
setCovValue(value, fit, method = NULL, ...)

\method{setCovValue}{default}(value, fit, method = NULL, ...)

\method{setCovValue}{matrix}(value, fit, method = NULL, ...)
}
\arguments{
\item{value}{the result to install}

\item{fit}{nlmixr2 fit}

\item{method}{requested covariance-method name, or \code{NULL}}

\item{...}{ignored by the built-in methods}
}
\value{
\code{list(cov, method, options)}, optionally with \code{extra}
}
\description{
The generic behind \code{setCov(fit) <- value}.  A method returns
\code{list(cov =, method =, options =)}: the covariance named like
\code{fit$cov}, the covariance-method name to install it as, and the
options recorded for it (the same key \code{setCovOptions()} gives the
control that would compute it, so \code{setCov()} can reuse it).  It may
also return \code{extra}, a named list of objects stored in the fit
environment (so \code{fit$<name>} returns them) once the covariance is
installed -- the full result the covariance came from, say.
}
\seealso{
\code{\link{setCov<-}}
}
\author{
Matt Fidler
}
