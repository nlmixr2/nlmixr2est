% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlmHessAnalytic.R
\name{rxUiGet.nlmHessEnv}
\alias{rxUiGet.nlmHessEnv}
\title{Build the symengine env with second-order theta sensitivities + hess lines}
\usage{
\method{rxUiGet}{nlmHessEnv}(x, ...)
}
\value{
symengine env with `..sens2` and `..hessLines`, or a character
  reason when out of scope
}
\description{
Starts from `rxUiGet.nlmHdTheta` (the same forward first-order env the
thetaGrad model uses; never the adjoint substitution in `rxUiGet.nlmEnv`).
Since `rx_pred_` is the per-observation -llik, the emitted
`rx_hess_<i>_<j>_` columns (i<=j triangle, j outer/i inner) are the
objective-Hessian contributions directly.
}
