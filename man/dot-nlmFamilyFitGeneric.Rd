% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlmShared.R
\name{.nlmFamilyFitGeneric}
\alias{.nlmFamilyFitGeneric}
\title{Shared fit driver for the nlm-family estimation methods}
\usage{
.nlmFamilyFitGeneric(
  env,
  method,
  fitModel,
  getTheta,
  controlToFocei,
  returnFlag,
  objective = NULL,
  message = function(fit) fit$message,
  emitFitWarnings = FALSE,
  extra = "",
  adjustOutput = TRUE,
  postSetup = NULL
)
}
\arguments{
\item{env}{dispatch environment (provides `ui`, `control`, `data`, `table`)}

\item{method}{estimation-method string; also the slot the raw fit is stored
under (e.g. `"nlm"` -> `.ret[["nlm"]]`)}

\item{fitModel}{`function(ui, dataSav)` running the optimizer}

\item{getTheta}{`function(fit, ui)` returning the full theta vector}

\item{controlToFocei}{`function(env)` translating the control to a
focei-style control for output assembly}

\item{returnFlag}{rxode2 control flag name that short-circuits and returns the
raw optimizer result (e.g. `"returnNlm"`)}

\item{objective}{optional `function(fit)` returning the raw objective; when
`NULL` the driver does not set `$objective` (a `postSetup` closure did)}

\item{message}{`function(fit)` returning the `$message` (default `fit$message`)}

\item{emitFitWarnings}{when TRUE, re-emit the warnings collected from
`fitModel` via `warning()` (nlm does this; the others do not)}

\item{extra}{`$extra` print string, or a `function(control)` returning it}

\item{adjustOutput}{when TRUE, run `.nlmFamilyAdjustOutput()`}

\item{postSetup}{optional `function(ret, ui, fitList)` returning a modified
`ret`, run right after the raw fit is stored and before
`.nlmFamilyAdjustOutput()` (for methods that set cov/covMethod/objective
with custom values)}
}
\value{
the assembled nlmixr2 fit (or the raw optimizer result if `returnFlag`)
}
\description{
Shared fit driver for the nlm-family estimation methods
}
\author{
Matthew L. Fidler
}
