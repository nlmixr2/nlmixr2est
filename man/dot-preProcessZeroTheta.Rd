% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preProcessZeroTheta.R
\name{.preProcessZeroTheta}
\alias{.preProcessZeroTheta}
\title{Nudge exactly-zero theta initial estimates off zero (FOCEi family)}
\usage{
.preProcessZeroTheta(ui, est, data, control)
}
\arguments{
\item{ui}{rxode2 ui model}

\item{est}{estimation method (all methods are shown by
`nlmixr2AllEst()`). Methods can be added for other tools}

\item{data}{nlmixr data}

\item{control}{The estimation control object.  These are expected
to be different for each type of estimation method}
}
\value{
list with the ui (possibly modified)
}
\description{
FOCEi scales a linear parameter by its native magnitude `|init|`, which
is `0` (no scale) when a population parameter is initialized at exactly
`0` (the parameter then freezes).  This hook moves every estimated,
non-fixed `theta` whose initial estimate is exactly `0` to `+zeroTheta`
when that is within its bounds, otherwise `-zeroTheta`; if neither is
within the bounds it errors.  Only runs for the FOCEi family (a
`foceiControl`), and runs before `.preProcessBoundedTransform` so the
nudged value is what gets transformed.
}
\author{
Matthew L. Fidler
}
